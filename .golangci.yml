version: "2"

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - misspell
    - unconvert
    - bodyclose
    - nilerr
    - errorlint

formatters:
  enable:
    - gofmt
    - goimports

linters-settings:
  misspell:
    locale: US
  errorlint:
    errorf: true
  goimports:
    local-prefixes: github.com/robert-crandall/gh-pr-feedback

issues:
  max-issues-per-linter: 0
  max-same-issues: 0

run:
  timeout: 5m
